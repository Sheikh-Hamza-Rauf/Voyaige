{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "nixpacks",
      "buildCommand": "echo building!"
    },
    "deploy": {
      "startCommand": "web: waitress-serve --port=5000 form:app",
      "restartPolicyType": "never",
      "healthcheckPath": "/",
      "healthcheckTimeout": 100
    }
  }
  